<%= form_with(model: meal) do |form| %>
  <% if meal.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(meal.errors.count, "error") %> prohibited this meal from being saved:</h2>

      <ul>
        <% meal.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%#       meal form%>
  <div class="form-wrapper">
    <div>
      <%= form.label :meal, style: "display: block" %>
      <%= form.text_field :meal, class:'form-control mb-2' %>
    </div>
    <div>
      <%= form.label :date, style: "display: block" %>
      <%= form.date_field :date, class:'form-control mb-2' %>
    </div>
  </div>

  <%#       create meal button%>
  <div>
    <%= form.submit class:'btn btn-primary mt-5' %>
  </div>
<% end %>
